services:
  postgres:
    image: postgres:16-alpine
    container_name: climbing_game_db
    environment:
      POSTGRES_DB: climbing_game
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
